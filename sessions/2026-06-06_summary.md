# Session — 2026-06-06 earth2guide (세션 10)

**시작**: ~12:45 KST / **종료**: ~17:50 KST

## 한 일
1. **Git 동기화 점검 + 정리**
   - local `main` == `origin/main` == Live(`36ae571`) 확인 — pull/push/migrate 할 것 없었음
   - 미커밋 13개 분석: 11개 = 샌드박스 CRLF 허상(내용 0자) → 폐기 / 2개 = 진짜 문서(ARCHITECTURE +56, CLAUDE +2) → 커밋 `60a500b`
   - stale git lock 2개(`index.lock`, `maintenance.lock`) + probe 잔여물 Windows에서 제거
2. **GSC + GA4 연결** (Alvin 제공: GA `G-F0PYH6DYLW`, GSC 토큰 `Y1-Ehof…`)
   - `web/app/layout.tsx`: `metadata.verification.google` + `next/script` gtag 추가 (공개 ID 하드코딩)
   - 빌드 통과 (665 페이지, TS 에러 0) → 커밋 `098f460` → push
3. **Vercel 배포 차단 발견 + 해결**
   - 모든 배포가 즉시 CANCELED → 원인: Ignored Build Step **"Don't build anything"**
   - Chrome으로 대시보드 진입 → Behavior **Automatic** 변경·저장 → "Ignore Build Step" 해제 Redeploy → **READY** (`dpl_47afctZea`)
4. **라이브 검증**: GSC meta + gtag 발동(dataLayer 4) 직접 확인
5. **GSC 마무리** (Chrome): URL-접두어 속성 생성 → 소유권 **자동 확인됨**(HTML 태그) → **sitemap.xml 제출** → 사이트맵 200/XML/358 URL 검증

## 결정사항
- web/ CRLF 허상 폐기 + 문서만 커밋 (옵션 A)
- **earth2guide만 작업** — 타 repo 불가침 (Alvin 지시)
- git 쓰기/fetch는 Windows PowerShell로 (샌드박스 마운트 unlink 불가)
- Vercel Behavior = **Automatic 유지** (자동배포 ON)
- GSC = URL-접두어 + HTML 태그 / 공개 ID 하드코딩

## 다음 세션 첫 행동
- GSC 색인·GA4 데이터 유입 확인 (며칠 소요) → 이상 없으면 HANDOFF 백로그 재개 (Step 8/9 자동화 빌드, Step 10 소식 탭 UI, Step 11 SEO meta)

## 미해결 이슈
- 트리거용 빈 커밋 `330859a` 히스토리에 존재 (정리 여부 Alvin 판단)
- GSC 옛 2022 WP 사이트맵 항목 오류 상태로 잔존 (무시 가능 / 삭제 선택)
- 타 repo: alvin-travel `.git/.git` 손상, k-skincare 453 등 대량 미커밋 (범위 밖, 미처리)
- 세션 종료 시점 기준 git working tree: CHANGELOG/CONTEXT/DECISIONS/sessions 변경분 **미커밋** (Session Push Git 요청 시 처리)
